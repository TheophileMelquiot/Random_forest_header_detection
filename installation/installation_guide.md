1. Un-zip the folder

2. go inside the final_module_ia or the new names you gave it via the terminal, and tap " pip install -e . ", it will install the package to your python make it available everywhere

3. open an python page and use it.

from excel_ai import detect_headers

headers = detect_headers("C:/Users/file.xlsx")


# Random_forest_header_detection
Creation, training of an random forest model to detect the header inside an excel document and comparaison with an Heuristic Scoring Algorithm model to compare the with one is more efficient for this task
